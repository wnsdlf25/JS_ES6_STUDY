// ``(백틱)을 사용하여
// - 문자열의 엔터키를 쓸 수 있다.
// - + 기호를 사용하여 변수를 넣기 쉽다.
// - ${ 변수 } 를 사용하여 중간중간에 변수를 넣을 수 있다.
var 변수 = " 안녕하세여";
var 변수2 = "반갑습니다";
var 문자 = 변수 + ` 최 준 일 ${변수2}`;

console.log(문자);
// -   안녕하세여 최 준 일 반갑습니다
